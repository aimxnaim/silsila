{
  "name": "silsilah",
  "private": true,
  "version": "1.0.0",
  "description": "A history engine for organisational structure. Reconstructs how roles and people changed over time.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "generate:data": "node scripts/generate-dataset.mjs",
    "verify": "node --experimental-strip-types scripts/verify-domain.mjs",
    "smoke": "node --experimental-strip-types scripts/smoke-test.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
